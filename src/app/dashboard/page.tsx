import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Инвентар",
};

export default async function DashboardInventoryPage() {
  return <h1>АДМИНИСТРАТОРСКИ ПАНЕЛ - ИНВЕНТАР</h1>;
}
