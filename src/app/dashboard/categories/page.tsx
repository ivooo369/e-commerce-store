import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Категории и подкатегории",
};

export default function DashboardCategoriesPage() {
  return <h1>АДМИНИСТРАТОРСКИ ПАНЕЛ - КАТЕГОРИИ И ПОДКАТЕГОРИИ</h1>;
}
