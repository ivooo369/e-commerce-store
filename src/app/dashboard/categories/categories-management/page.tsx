import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Категории и подкатегории",
};

export default function DashboardCategoriesManagementPage() {
  return (
    <h1>АДМИНИСТРАТОРСКИ ПАНЕЛ - КАТЕГОРИИ И ПОДКАТЕГОРИИ (МЕНАЖИРАНЕ)</h1>
  );
}
