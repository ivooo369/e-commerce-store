import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Редактиране на продукт",
};

export default function DashboardEditProductPage() {
  return <h1>АДМИНИСТРАТОРСКИ ПАНЕЛ - РЕДАКТИРАНЕ НА ПРОДУКТ</h1>;
}
